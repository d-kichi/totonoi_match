<!DOCTYPE html>
<html>
  <head>
    <title>ととのいmatch!!</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body class="bg-light">
    <!-- ✅ ナビバー -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
      <div class="container">
        <%= link_to "ととのいmatch!!", reset_diagnosis_path, class: "navbar-brand fw-bold text-primary" %>

        <div class="ms-auto d-flex align-items-center gap-2">
          <%= link_to '最初から診断', new_diagnosis_path, class: 'btn btn-outline-primary' %>

          <% if user_signed_in? %>
            <%= link_to "マイページ", mypage_path, class: "btn btn-outline-secondary" %>
            <%= link_to "ログアウト", destroy_user_session_path, class: "btn btn-outline-danger", data: { turbo_method: :delete } %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-secondary" %>
            <%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </nav>
<% if notice %>
  <div class="alert alert-success text-center"><%= notice %></div>
<% elsif alert %>
  <div class="alert alert-danger text-center"><%= alert %></div>
<% end %>
    <!-- ✅ メイン -->
    <main class="container py-5 mt-5">
      <%= yield %>
    </main>

    <!-- ✅ フッター -->
    <footer class="text-center py-3 border-top text-muted small bg-white">
  © 2025 Totonoi Match | 心ととのう、最高のサウナ体験を。|
  <%= link_to "利用規約", terms_path, class: "text-decoration-none text-muted mx-2" %> |
  <%= link_to "プライバシーポリシー", privacy_path, class: "text-decoration-none text-muted mx-2" %> |
  <%= link_to 'お問い合わせ', new_contact_path, class: 'text-decoration-none text-muted mx-2' %>
</footer>
  </body>
</html>
